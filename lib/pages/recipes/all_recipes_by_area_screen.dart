import 'package:flutter/cupertino.dart';

class AllRecipesByAreaScreen extends StatelessWidget {
  const AllRecipesByAreaScreen({Key? key, this.areas}) : super(key: key);
  final areas;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
